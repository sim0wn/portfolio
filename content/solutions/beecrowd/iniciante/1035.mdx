---
title: Teste de Seleção 1
url: https://judge.beecrowd.com/pt/problems/view/1035
category: Iniciante
---

## Descrição

Leia 4 valores inteiros A, B, C e D. A seguir, se B for maior do que C e se D for maior do que A, e a soma de C com D for maior que a soma de A e B e se C e D, ambos, forem positivos e se a variável A for par escrever a mensagem "Valores aceitos", senão escrever "Valores nao aceitos".

## Solução
Para a solução desse exercício, basta receber 4 entradas do usuário e então imprimir na tela o resultado de acordo com as condições especificadas.

```py title="main.py"
# -*- coding: utf-8 -*-

A, B, C, D = map(int, input().split(" "))

if B > C and D > A and C + D > A + B and C >= 0 and D >= 0 and not A % 2:
    print("Valores aceitos")
else:
    print("Valores nao aceitos")
```